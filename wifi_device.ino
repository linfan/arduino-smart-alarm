//#include <Process.h>
#include "wifi_device.h"

void WifiDevice::init()
{
//  Bridge.begin();
}

char* WifiDevice::httpGet(char* url)
{
//  Process p;        // Create a process and call it "p"
//  p.begin("curl");  // Process that launch the "curl" command
//  //p.addParameter("--retry 1");
//  p.addParameter("--max-time 9");
//  p.addParameter(url); // Add the URL parameter to "curl"
//  p.run();      // Run the process and wait for its termination
//
//  // Process output can be read with the stream methods
//  unsigned int pos = 0;
//  while (p.available()>0) {
//    char c = p.read();
//    buffer[pos++] = c;
//    if (pos >= CURL_BUFFER_SIZE - 1)
//        break;
//  }
//  buffer[pos] = '\0';

  return "";
}













